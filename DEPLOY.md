# Деплой на Zeabur - Пошаговая инструкция

## Быстрый старт (5 минут)

### Шаг 1: Открой Zeabur
Перейди на https://zeabur.com и войди в свой аккаунт

### Шаг 2: Создай новый проект
1. Нажми **"Create New Project"**
2. Выбери регион (например, **US West** или ближайший к тебе)
3. Дай проекту имя: `ClickUp MCP Server`

### Шаг 3: Добавь сервис из GitHub
1. Нажми **"Add Service"**
2. Выбери **"Git"**
3. Найди и выбери репозиторий: **`retailbox-automation/clickup-mcp`**
4. Нажми **"Deploy"**

### Шаг 4: Настрой переменные окружения
1. После деплоя перейди в настройки сервиса
2. Найди раздел **"Environment Variables"**
3. Добавь переменную:
   ```
   CLICKUP_API_KEY=твой_clickup_токен_здесь
   ```
4. Добавь еще одну (опционально):
   ```
   PORT=8000
   ```
5. Нажми **"Save"** и **"Redeploy"**

### Шаг 5: Получи URL деплоя
1. В интерфейсе Zeabur найди раздел **"Domains"**
2. Zeabur автоматически создаст домен типа: `your-app-xxxxx.zeabur.app`
3. Твой MCP endpoint будет: `https://your-app-xxxxx.zeabur.app/mcp`

### Шаг 6: Проверь работоспособность
Попробуй открыть endpoint в браузере: `https://your-app-xxxxx.zeabur.app/mcp`

Должен вернуться JSON с информацией о сервере или ошибка подключения (это нормально для браузера)

---

## Подключение к Web Claude

1. Открой https://claude.ai
2. Перейди в **Settings → Integrations**
3. Найди секцию **"Model Context Protocol (MCP)"**
4. Добавь новый сервер:
   ```json
   {
     "clickup": {
       "url": "https://your-app-xxxxx.zeabur.app/mcp"
     }
   }
   ```

   **Примечание**: Не указывай `transport`, Claude автоматически определит HTTP Stream
5. Сохрани и перезагрузи страницу

---

## Получение ClickUp API Key

1. Зайди на https://app.clickup.com/settings/apps
2. Найди секцию **"API Token"**
3. Нажми **"Generate"** (если токена еще нет)
4. Скопируй токен (начинается с `pk_`)
5. **Важно**: Никому не показывай этот токен!

---

## Проверка логов

Если что-то не работает:
1. В Zeabur открой свой сервис
2. Перейди в раздел **"Logs"**
3. Посмотри на ошибки

Частые проблемы:
- ❌ **"CLICKUP_API_KEY environment variable is not set"** → Не установлена переменная окружения
- ❌ **"Authentication failed"** → Неверный API токен
- ❌ **"Port already in use"** → Обычно Zeabur сам решает, можно не беспокоиться

---

## Использование в Claude

После подключения попробуй команды:
- "Покажи мои ClickUp workspace"
- "Какие spaces есть в workspace с ID XXX?"
- "Покажи детали space с ID YYY"
- "Какие custom fields есть в списке ZZZ?"

---

## Обновление кода

Когда нужно обновить сервер:
1. Внеси изменения в код
2. Закоммить и запушь в GitHub:
   ```bash
   git add .
   git commit -m "Update: описание изменений"
   git push
   ```
3. Zeabur автоматически задеплоит новую версию!

---

## Полезные ссылки

- 📖 [Zeabur Documentation](https://zeabur.com/docs)
- 🔧 [ClickUp API Docs](https://developer.clickup.com)
- 🤖 [MCP Protocol](https://modelcontextprotocol.io)
- 📦 [Репозиторий](https://github.com/retailbox-automation/clickup-mcp)

---

Готово! Если возникнут вопросы - смотри логи в Zeabur или проверь README.md в репозитории.
